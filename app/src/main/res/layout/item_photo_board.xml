<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="2dp">

    <!-- Foto cuadrada -->
    <ImageView
        android:id="@+id/ivFoto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:contentDescription="@string/foto_de_recibo" />

    <!-- Overlay semitransparente cuando está seleccionada -->
    <View
        android:id="@+id/viewSelectionOverlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#661565C0"
        android:visibility="gone" />

    <!-- Checkmark de selección -->
    <ImageView
        android:id="@+id/ivCheckmark"
        android:layout_width="28dp"
        android:layout_height="28dp"
        android:layout_gravity="top|end"
        android:layout_margin="6dp"
        android:src="@drawable/ic_check_circle"
        android:visibility="gone"
        android:contentDescription="@string/seleccionada"
        app:tint="@android:color/white" />

</FrameLayout>