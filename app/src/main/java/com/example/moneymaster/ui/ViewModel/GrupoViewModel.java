package com.example.moneymaster.ui.ViewModel;

import android.app.Application;
import androidx.annotation.NonNull;
import androidx.lifecycle.AndroidViewModel;

/**
 * ViewModel completo de grupos — implementación pendiente.
 * Se completará en cards futuros (Card #26+) cuando GrupoRepository
 * tenga todos los métodos necesarios.
 */
public class GrupoViewModel extends AndroidViewModel {

    public GrupoViewModel(@NonNull Application application) {
        super(application);
    }
}
